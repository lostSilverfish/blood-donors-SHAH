# Production Environment Configuration Template
# Copy this file to backend/config.env and fill in your actual values

# Server Configuration
PORT=3000
NODE_ENV=production

# Database Configuration (Replace with your production database details)
DB_HOST=your-database-host
DB_USER=your-database-user
DB_PASSWORD=your-secure-database-password
DB_NAME=blood_donor_registry
DB_PORT=3306

# JWT Configuration (MUST be changed for production)
JWT_SECRET=your-super-secure-jwt-secret-key-minimum-32-characters
JWT_EXPIRES_IN=7d

# CORS Configuration (Add your production domain(s))
ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Frontend URL (Your production domain)
FRONTEND_URL=https://yourdomain.com 